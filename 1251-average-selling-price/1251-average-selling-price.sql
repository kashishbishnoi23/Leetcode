# Write your MySQL query statement below
select P.product_id, IFNULL(ROUND(SUM(U.units*P.price)/SUM(U.units), 2), 0) as average_price from UnitsSold U right join Prices P on U.product_id = P.product_id AND U.purchase_date BETWEEN P.start_date AND P.end_date group by P.product_id;